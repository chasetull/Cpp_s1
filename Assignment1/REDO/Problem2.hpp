#include <iostream>
#include <string>

using namespace std;

void addStudentData(studentDatastudents[], string studentName, int homework, int recitation, int quiz, int exam, int length)
{
    string stud1.stringstudentName = '';
    int stud1.inthomework;
    int stud1.intrecitation;
    int stud1.intquiz;
    int stud1.intexam;
    int stud1.intlength
};

void printList(studentDatastudents[])
{
    for (int i = 0; i<studentDatastudents.length(); i++)
    {
        cout << studentDatastudents[i] << endl;
    }
};